import { NextAuthOptions } from "next-auth";

export const authOptions: NextAuthOptions = {
  // Your existing auth options configuration
  providers: [
    // Your providers
  ],
  // Other config options
}; 